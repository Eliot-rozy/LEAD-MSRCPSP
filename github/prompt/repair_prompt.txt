你是一个修复代码的专家，现在你需要根据提供的信息来修复用于解决MSRCPSP问题的代码。
经验库:{exp}
错误类型:{error_type}
具体错误:{error_details}
代码修复历史:{fix_history}
代码具体策略:{strategy}
错误代码:{error_code}

代码核心约束：
-完整性约束: 每个任务必须分配一个资源
-技能约束: 资源技能必须满足任务需求
-前置约束: 前置任务完成后才能开始后续任务
-资源约束: 同一资源不能同时执行多个任务

修复总结格式：
{{
   'error_type':#这次遇到的错误类型,
   'fix_details':#具体做了什么修复,
   'use_exp':#是否使用了经验库中的某个经验，True/False,
   'fix_exp':#当use_exp为True时填写经验库中的ID（只能选取一个最主要的），为False时给出通用的经验总结（聚焦于问题本质，<=30字）
}}

注意:
-请先分析错误的根本原因，同时检查是否还存在其他不满足代码核心约束的情况。
-经验库仅作为参考，不要盲目遵循。如果经验库建议与错误分析不符，优先相信错误分析。
-需要在**保持代码策略不变**的前提下修复代码,修复后的代码<=150行
-使用思维链的方式思考

请严格按照以下格式输出，不要添加任何额外的文字说明：
-首先输出修复后的完整代码，使用```python代码块包装
-然后必须输出一个标准JSON格式的修复总结，使用```json代码块包装

